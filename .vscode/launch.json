{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug Active File in Brave",
            "file": "${file}",
            "runtimeExecutable": "/usr/bin/brave-browser"
        }
    ]
}
